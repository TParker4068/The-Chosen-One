using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "PlayerChars", menuName = "PlayerChars/Create New PlayerChar")]
public class PlayerCharacterBase : UnitBase
{
    
    // Update is called once per frame
    void Update()
    {
        
    }
}
